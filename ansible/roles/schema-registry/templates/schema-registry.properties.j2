# Maintained by Ansible
kafkastore.connection.url={% for host in groups['zookeeper'] %}{% if loop.index > 1 %},{% endif %}{{ host }}:2181{% endfor %}

{% for key, value in schema.registry.config.items() %}
{{ key }}={{ value }}
{% endfor %}
